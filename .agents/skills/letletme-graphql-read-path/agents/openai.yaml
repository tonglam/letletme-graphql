interface:
  display_name: "LetLetMe GraphQL Read Path"
  short_description: "Trace ingress, authorization, reads, caches, and contracts"
  default_prompt: "Audit or change this LetLetMe GraphQL read path with repository-specific trust and contract gates."
